// Plugin
export { tokenProgramPlugin } from './token-program-plugin';

// Core (for direct use without plugin)
export { createTokenProgramNamespace } from './token-program';

// Signer resolution
export { resolveSigner } from './resolve-signer';

// Types
export type { TokenProgramClientRequirements, TokenProgramNamespace } from './types';
